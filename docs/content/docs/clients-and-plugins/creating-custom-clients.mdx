---
title: Creating custom clients
description: Compose plugins into reusable client factories
---

<Callout type="warn" title="Planned Content">
**Status:** New page

This page will cover when to create a custom client vs using a ready-made one, building a client from scratch with `createEmptyClient().use(...).use(...)`, wrapping the composition in a factory function with typed config, example custom clients for specific use cases (e.g. read-only client, client with custom retry logic), returning proper TypeScript types so consumers get full autocompletion, and how the official clients (`createClient`, `createLocalClient`) are implemented (source walkthrough).

**Draws from:** `@solana/kit-client-rpc` source (as an example of a well-structured client factory), `@solana/plugin-core` docs, kit-plugins main README.

**Links to:** [Creating Custom Plugins](/docs/clients-and-plugins/creating-custom-plugins), [Available Clients](/docs/clients-and-plugins/available-clients), [Advanced Guides — Kit Without a Client](/docs/advanced-guides/kit-without-a-client).

</Callout>
