---
'@solana/instruction-plans': minor
'@solana/errors': minor
---

Add a function `getInstructionsFromInstructionPlan` that returns the instructions from an `InstructionPlan`, including when they are nested.
